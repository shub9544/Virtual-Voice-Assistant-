# Virtual-Voice-Assistant-
A Virtual Voice Assistant built using Python, pyttsx3, Microsoft SAPI (SAPI5), and PyQt that provides an interactive desktop experience with voice output capabilities.

The assistant uses the Windows SAPI engine through pyttsx3 to convert text responses into natural speech and features a user-friendly GUI developed with PyQt. It works offline and can process user commands to deliver spoken responses efficiently.
